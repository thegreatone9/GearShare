# ============================================================
# GearShare — Non-secret defaults (safe to commit)
# ============================================================
# Secrets (API keys, DB URLs, passwords) belong in .env.local
# which is gitignored. See .env.example for the full template.
# ============================================================

# Supabase project URL (public, not a secret)
VITE_SUPABASE_PROJECT_URL=https://tmuiycpixqjawkspqpiu.supabase.co